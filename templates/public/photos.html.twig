{% extends 'base.html.twig' %}


{% block stylesheets %}
{{parent()}}
<style media="screen">
    .row {
      display: flex;
      flex-wrap: wrap;
      padding: 0 4px;
    }

    /* Create four equal columns that sits next to each other */
    .column {
      flex: 33%;
      max-width: 33%;
      padding: 0 4px;
    }

    .column img {
      margin-top: 8px;
      vertical-align: middle;
      width: 100%;
    }

    /* Responsive layout - makes a two column-layout instead of four columns */
    @media screen and (max-width: 800px) {
      .column {
        flex: 40%;
        max-width: 40%;
      }
    }

    /* Responsive layout - makes the two columns stack on top of each other instead of next to each other */
    @media screen and (max-width: 600px) {
      .column {
        flex: 100%;
        max-width: 100%;
      }
    }
</style>
{% endblock %}

{% block body %}
<div class="row">
  <div class="column">
    <img src="{{asset('build/lambarene/campfire.jpg')}}">
    {{ 'photo.fireplace' | trans }}
    <img src="{{asset('build/lambarene/building_outside.jpg')}}">
    {{ 'photo.outside' | trans }}
    <img src="{{asset('build/lambarene/building_outside_2.jpg')}}">
    {{ 'photo.outside' | trans }}
    <img src="{{asset('build/lambarene/entrance.jpg')}}">
    {{ 'photo.entrance' | trans }}
  </div>
    <div class="column">
      <img src="{{asset('build/lambarene/right_room.jpg')}}">
      {{ 'photo.radixzaal' | trans }}
      <img src="{{asset('build/lambarene/fridge.jpg')}}">
      {{ 'photo.fridge' | trans }}
      <img src="{{asset('build/lambarene/kitchen.jpg')}}">
      {{ 'photo.kitchen' | trans }}
      <img src="{{asset('build/lambarene/toilet_kitchen.jpg')}}">
      {{ 'photo.hall' | trans }}
    </div>
      <div class="column">
        <img src="{{asset('build/lambarene/radix_hok_oud.jpg')}}">
        {{ 'photo.radixzaal_oud' | trans }}
        <img src="{{asset('build/lambarene/toilets.jpg')}}">
        {{ 'photo.toilets' | trans }}
        <img src="{{asset('build/lambarene/shower.jpg')}}">
        {{ 'photo.shower' | trans }}
        <img src="{{asset('build/lambarene/left_room.jpg')}}">
        {{ 'photo.anderezaal' | trans }}
      </div>
</div>
{% endblock %}
