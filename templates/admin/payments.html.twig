{% extends 'admin/layout.html.twig' %}


{% block main %}

<div>
    <table class="table table-striped table-middle-aligned">
        <!-- <col width="100px">
        <col width="80">
        <col width="10"> -->
        <tr>
            <th width="60%" >{{'label.association' | trans }} ({{'label.price' | trans }} - {{'label.deposit' | trans }})</th>
            <th width="30%" >{{'label.paid' | trans }}</th>
            <th width="10%" >{{'label.deposit_retour' | trans }}</th>
        </tr>
        {{ form_start(form) }}
        {% for key, req in form.requests %}
                <tr>
                    {% set request = attribute(task.requests, key) %}
                    <td width="60%" >{{ request.association }} (€{{request.price | number_format(2,',')}} - €{{request.deposit | number_format(2,',')}})</td>
                    <td width="30%" >{{ form_row(req.paid) }}</td>
                    <td width="10%" >{{ form_row(req.deposit_retour) }}</td>
                </tr>
        {% endfor %}
        {{ form_row(form.submit) }}
        {{ form_end(form) }}
    </table>
</div>
<center>
    <div>
        <table>
            <tr>
                {% for year in years %}
                    <th>
                        <form action="{{ path('admin_payments_overview', {'year': year })}}">
                            <input type="submit" class="btn btn-primary" value="{{year}}" />
                        </form>
                    </th>
                {% endfor %}
        </table>
    </div>
    </center>

{% endblock %}

{% block stylesheets %}
{{ parent()}}
{% endblock %}
